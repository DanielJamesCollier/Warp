+ sync environment strings
+ write .exe and .obj files to warp/servers/artifacts. maybe we need a folder per build / per compile command.
+ If the remote compile fails. retry on the client. 
+ handle clang-cl.exe main.c two.c
